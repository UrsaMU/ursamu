/** Package identity for staff nav and freeze docs. */
export const WIKI_VERSION = "0.2.8";

/** Technical plugin id (IPlugin.name, routes). */
export const WIKI_PLUGIN_ID = "wiki";

/**
 * Short title for staff topbar + page H1.
 * Keep short — top tabs are dense.
 */
export const WIKI_TITLE = "Wiki";

/** One-line operator blurb. */
export const WIKI_DESCRIPTION =
  "Markdown wiki — pages, drafts, locks, and history.";
