# @ursamu/mail

In-game mail system plugin for UrsaMU.

## Configuration

This plugin supports custom database collection names. In your `config.json`,
you can customize the collection name:

```json
{
  "plugins": {
    "mail": {
      "db": "mail.messages"
    }
  }
}
```
