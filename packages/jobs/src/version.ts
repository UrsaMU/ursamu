/** Package identity for staff nav and freeze docs. */
export const JOBS_VERSION = "1.1.2";

/** Technical plugin id (IPlugin.name, routes, DBO prefix). */
export const JOBS_PLUGIN_ID = "jobs";

/**
 * Short title for staff topbar + page H1.
 * Keep short — top tabs are dense.
 */
export const JOBS_TITLE = "Jobs";

/** One-line operator blurb. */
export const JOBS_DESCRIPTION =
  "Request queue — triage, assign, comment, and close.";
