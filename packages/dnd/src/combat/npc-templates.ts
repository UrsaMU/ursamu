/**
 * NPC templates — re-export from JSON catalog.
 */
export type {
  DropConfig,
  NpcTemplate,
} from "../data/catalog.ts";
export {
  NPC_TEMPLATES,
  npcBySlug,
  listNpcSlugs,
} from "../data/catalog.ts";
