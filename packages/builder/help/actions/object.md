See also: +help actions (overview)

+OBJECT

  General object-triggered attributes (startup, destroy,
  and similar engine hooks when present).

NOTES
  Not all MUX attr names exist on UrsaMU. Examine the object
  and engine docs for supported slots.

SEE ALSO: +help actions/overview, +help objects/examine
