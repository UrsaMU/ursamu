# Utopia theme

RetroWave tokens on the `@ursamu/site` shell.

```bash
deno task studio   # Theme Studio
deno task pack     # → utopia.zip
```

Do not rename `.site-shell` / `.play-*` classes. Override
`--site-*` only.
