/** Package identity for +bbversion, staff nav, and freeze docs. */
export const BBS_VERSION = "1.1.0";
export const BBS_CODENAME = "Myrddin";

/** Technical plugin id (IPlugin.name, routes, DBO prefix). */
export const BBS_PLUGIN_ID = "bbs";

/**
 * Short title for staff topbar + page H1.
 * Keep short — top tabs are dense (Wiki, Jobs, Boards).
 */
export const BBS_TITLE = "Boards";

/** One-line operator blurb (IPlugin.description + page lede). */
export const BBS_DESCRIPTION =
  "Bulletin boards — threading, categories, sticky " +
  "posts, moderators, and jobs-bridge.";
