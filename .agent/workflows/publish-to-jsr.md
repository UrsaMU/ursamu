---
description: Publish the UrsaMU package to JSR
---

1. Ensure you are logged into JSR:
   ```bash
   deno publish --dry-run --allow-dirty
   ```
   (If this works, you are good. If not, follow the auth prompt).

2. Publish the package:
   ```bash
   // turbo
   deno publish
   ```
