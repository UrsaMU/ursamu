node_modules
data
dist
WIP
db
db
.pup.*
# Ignore config files but keep the directory structure
config/*.json
# Don't ignore the README and .gitkeep
!config/README.md
!config/.gitkeep

# Ignore skills repository
.agent/skills
.agents/skills
