node_modules
# Root game DB only. Package tables live in packages/*/data.
/data
dist
WIP
# Root runtime DB only. Package schemas live in packages/*/db.
/db
.pup.*
logs/

# Runtime databases generated by local games
**/typegraph.db/
**/ursamu.db
*.db-wal
*.db-shm

# Runtime secrets — never commit
.jwt_secret
.env
.env.*
!.env.example

# Ignore config files but keep the directory structure
config/*.json
# Don't ignore the README and .gitkeep
!config/README.md
!config/.gitkeep

# Ignore skills repository
.agent/skills
.agents/skills

# Claude Code local settings and agent worktrees
.claude/

# Fresh build directory
_fresh/
games/
scratch/
types/deno.d.ts
types/@std/
