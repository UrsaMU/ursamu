@UNLOCK

COMMAND: @unlock[/<switch>] <object>

Removes the specified lock from <object>. If no switch is provided, the Basic
lock is removed.

Switches: (Same as @lock)

Example: @unlock me @unlock/enter home

Related Topics: @lock, locks
