# @OPEN

Creates a new exit.

## Syntax

`@open <exit name>=<destination DBREF>` `@open <exit name>`

## Description

Creates a new exit in the current room. If a destination is provided (via
DBREF), the exit is linked to that room.

## Example

`@open North=#123` `@open Out`
